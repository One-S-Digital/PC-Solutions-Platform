import React from 'react';

/**
 * Debugger is now always enabled - this component is no longer needed
 * Kept for backward compatibility
 */
const FrontendDebugToggle: React.FC = () => {
  return null;
};

export default FrontendDebugToggle;

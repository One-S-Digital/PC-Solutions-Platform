cd -- "$(dirname -- "$0")/.." || {
  echo "Error: Failed to change to repository root directory"
  exit 1
}
# Validate translation files before commit
node scripts/validate-translations.mjs

# Run linting on staged files (optional - can be slow)
# pnpm lint
